PREFIX wd: <https://compoundcloud.wikibase.cloud/entity/>
PREFIX wdt: <https://compoundcloud.wikibase.cloud/prop/direct/>

SELECT (SUBSTR(STR(?cmp), 45) AS ?start) (STR(?url) AS ?end) ?type WHERE {
  ?cmp wdt:P1 wd:Q2 .
  {
    ?cmp wdt:P4 ?url .
  }
  BIND ("web page" AS ?type)
}
